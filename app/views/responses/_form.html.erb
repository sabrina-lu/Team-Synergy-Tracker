<%= form_with(model: response, local: true) do |form| %>
  <% if response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(response.errors.count, "error") %> prohibited this response from being saved:</h2>

      <ul>
        <% response.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :survey_id %>
    <%= form.number_field :survey_id %>
  </div>

  <div class="field">
    <%= form.label :question_number %>
    <%= form.number_field :question_number %>
  </div>

  <div class="field">
    <%= form.label :response %>
    <%= form.number_field :response %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
