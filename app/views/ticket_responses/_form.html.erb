<%= form_with(model: ticket_response, local: true) do |form| %>
  <% if ticket_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_response.errors.count, "error") %> prohibited this ticket_response from being saved:</h2>

      <ul>
        <% ticket_response.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
